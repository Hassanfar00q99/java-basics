let gameName = "hassan"
const endGame = "loser"
console.log(`you are losser ${endGame} you are ${gameName}`);


let testing =new String("pakistan")
console.log(gameName.charAt(4));
console.log(gameName.indexOf("a"));
console.log(gameName.toLocaleUpperCase());
console.log(gameName.anchor());
console.log(gameName.at());
console.log(gameName.bold());
console.log(gameName.substring(1,4));
console.log(testing.slice(-7,4));
console.log(gameName.slice(-5,4));
console.log(gameName.slice(1,4));
// console.log(gameName);
// For example, consider the string "pakistan": 
// slice kesy kam krta hai uska method hai 
// "p" is at index 0,
// "a" is at index 1,
// "k" is at index 2,
// "i" is at index 3,
// "s" is at index 4,
// "t" is at index 5,
// "a" is at index 6,
// "n" is at index 7.
// If we were to count from the end:

// "n" is at index -1,
// "a" is at index -2,
// "t" is at index -3,
// "s" is at index -4,
// "i" is at index -5,
// "k" is at index -6,
// "a" is at index -7,
// "p" is at index -8.

testing.replace("pakistan","I love my pakistan" )
console.log(testing.replace("pakistan","I love my pakistan" ));
console.log(testing);
let splittesting = "i love you "
console.log(splittesting.split("o",4));
console.log(splittesting.blink());